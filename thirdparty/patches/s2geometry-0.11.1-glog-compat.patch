--- a/src/s2/base/log_severity.h
+++ b/src/s2/base/log_severity.h
@@ -18,14 +18,8 @@

 #include "absl/base/log_severity.h"

-// Stay compatible with glog.
-namespace google {
-
-#ifdef NDEBUG
-constexpr bool DEBUG_MODE = false;
-#else
-constexpr bool DEBUG_MODE = true;
-#endif
-
-}  // namespace google
+// StarRocks patch: removed S2's DEBUG_MODE definition to avoid conflict
+// with glog's log_severity.h which defines DEBUG_MODE as an anonymous
+// enum constant in namespace google. StarRocks already uses glog which
+// provides google::DEBUG_MODE.

 #endif  // S2_BASE_LOG_SEVERITY_H_
